import React from "react";
import QuizAttempt from "@/app/quiz/attempt/Attempt";

export const dynamic = "force-dynamic";

const Page = async () => {
  return <QuizAttempt />;
};

export default Page;
